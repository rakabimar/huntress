# Skill router (spec §16–§17).
#
# Maps categories -> skills.  Each skill lives in skills/<slug>/SKILL.md and its
# frontmatter carries the authoritative per-skill metadata (maturity,
# risk_class, category, cwe).  This file is the *router*: it names the library
# so the skill selector and `harness skill list/validate` can discover it, and
# it is what `harness sync` uses to copy skills into each runtime's skill dir.

categories:
  core:
    label: Core methodology (research loop + specialist stances)
    skills:
      - observe
      - hypothesize
      - research-loop
      - feature-threat-model
      - exploit-chain-analysis
      - attack
      - defend
      - triage
      - validate
      - report

  injection:
    label: Injection
    skills:
      - sql-injection
      - nosqli
      - command-injection
      - xss
      - xxe
      - ssti
      - ldap-injection
      - header-injection

  authnz:
    label: Authentication & authorization
    skills:
      - authentication
      - jwt
      - oauth-oidc
      - api-authorization
      - access-control
      - privilege-escalation
      - session-fixation
      - session-management

  api:
    label: API and GraphQL
    skills:
      - graphql

  business-logic:
    label: Business logic
    skills:
      - business-logic
      - race-condition
      - mass-assignment
      - payment-logic
      - workflow-bypass

  file:
    label: File handling
    skills:
      - file-upload
      - path-traversal
      - lfi
      - unrestricted-download

  web:
    label: CSRF / SSRF / redirect
    skills:
      - csrf
      - ssrf
      - open-redirect

  client-side:
    label: Client-side
    skills:
      - prototype-pollution
      - cors-misconfig
      - postmessage
      - websocket

  infra:
    label: Infrastructure & protocol
    skills:
      - subdomain-takeover
      - request-smuggling
      - host-header
      - cache-poisoning
      - cache-deception

  crypto-config:
    label: Cryptography & configuration
    skills:
      - crypto-misuse
      - information-disclosure
      - insecure-deserialization
      - rate-limit-bypass

  recon:
    label: Enumeration & fingerprinting
    skills:
      - recon-passive
      - api-enumeration

  whitebox:
    label: Commit-pinned source and open-source security research
    skills:
      - source-audit-context
      - source-dataflow-analysis
      - source-authorization-analysis
      - git-history-security
      - differential-security-review
      - variant-analysis
      - dependency-reachability
      - secret-exposure-analysis

  protocol:
    label: Identity, messaging, and parser protocols
    skills:
      - saml-sso
      - webhook-security
      - http-parameter-pollution
      - grpc-security

  platform:
    label: Capability-gated platform surfaces
    skills:
      - cicd-security
      - cloud-security
      - llm-ai-security
      - client-reverse
      - mobile-security

  specialized:
    label: Optional local specialized research
    skills:
      - fuzzing
