# SSRF evidence contract

Include input URL redacted as needed, parse/canonicalization expectation, validator decision if observable, unique callback token fingerprint, source/timestamp/header characteristics, DNS/redirect chain, final connected destination/capability, and baseline/refuting control. Redact internal banners, credentials, metadata tokens, and unrelated callback traffic.
