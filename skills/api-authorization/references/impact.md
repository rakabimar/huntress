# Authorization impact

Describe the demonstrated capability: read which protected fields; change which protected state; invoke which privileged action; cross which tenant/relationship; or obtain which export/job artifact. Scope impact to tested object classes and principals. Do not extrapolate one record to the database or predictable IDs to mass compromise.
