# Authorization remediation

Enforce object, tenant, relationship, and protected-field policy in the shared service/data-access operation used by every route, resolver, job, and version. Include ownership/tenant predicates in retrieval/update queries, deny protected input fields, authorize every batch member and generated artifact, and re-evaluate lifecycle changes. Add principal-matrix regression tests for sibling methods and representations.
