# Business-logic false positives

Reject cosmetic step skipping, documented retry/replay, idempotent duplicate responses, eventual consistency with one final effect, client-calculated values that the server recomputes, intentionally transferable/shared capabilities, allowed promotions, and test/sandbox provider artifacts. Establish the actual business rule before calling behavior forbidden.
