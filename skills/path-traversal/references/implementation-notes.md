# Implementation notes

Check Node path.resolve/relative containment, Python pathlib/os.path commonpath, Java Path normalize/toRealPath, Go filepath.Clean/Rel, PHP realpath, Ruby Pathname, and archive library defaults. Prefix string checks are unsafe without component boundaries. WordPress handlers need capability+nonce plus strict upload/base-directory containment.
