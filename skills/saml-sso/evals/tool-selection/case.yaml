name: tool-selection-saml-sso
input: "The SP supports two IdPs. Both controlled accounts have the same email. One assertion preview selects the same local profile, but the ACS has not issued a session."
expected:
  tool_selection: policy_preflight
  evidence_required: true
  max_requests: 0
