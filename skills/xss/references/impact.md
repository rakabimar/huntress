# XSS impact

State which controlled viewer executes attacker content, under which origin and prerequisites, and the minimal demonstrated browser capability. Distinguish same-user, privileged viewer, cross-tenant, stored, and reflected delivery. Do not assume cookie theft or account takeover from alert/DOM execution.
