# Implementation notes

Review Jinja/Django templates, Twig, FreeMarker/Velocity, Thymeleaf expression modes, ERB, Handlebars server compilation, Go templates, and custom formatters. Functions named render are not automatically vulnerable; find whether untrusted bytes reach template compilation rather than variables. Template sandbox claims require configuration and version review.
