# Report pattern

Describe the identity-binding chain, exact transition, intended subject, substituted binding, and authenticated outcome. Reproduction uses only controlled accounts and token fingerprints. Include a normal owner transaction and mismatched control, then show resulting local account/session state and lifecycle applicability.
