# Authentication impact

State whether the demonstrated outcome creates an authenticated session as the wrong account, changes a credential/factor, verifies an attacker-controlled identity, links an external identity, bypasses required step-up, or exposes a stable account oracle. Do not call a reset-message difference account takeover.
