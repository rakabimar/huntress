# Authentication evidence contract

Include the state transition, intended identity and proof channel, artifact fingerprint/purpose/issuer/consumer/expiry/use state, initiating and consuming transaction/browser, one changed binding, failure control, resulting local account/session identity, and protected capability. Redact token, code, email, phone, password, cookie, and PII values.
