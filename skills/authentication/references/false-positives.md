# Authentication false positives

Reject unstable timing/enumeration signals, token parsing followed by rejection, optional verification that grants no protected capability, magic links intentionally usable across the owner's devices while still subject-bound, documented linking after recent authentication, and client navigation that never issues an authenticated server session.
