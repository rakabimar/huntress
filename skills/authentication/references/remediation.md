# Authentication remediation

Bind every proof artifact to purpose, subject, transaction, consumer, short expiry, and atomic single use. Keep identity normalization consistent across channels, use issuer+stable subject for federation, require recent strong re-authentication for linking/factor changes, invalidate related artifacts/sessions on security events, and regression-test alternate endpoints and lifecycle replays.
