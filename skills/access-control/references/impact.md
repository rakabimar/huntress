# Access-control impact

Name the capability gained, the role that should be required, the lower role that exercised it, the affected organization/workspace, and the exact protected action/data. Separate broad function access from any object-specific reach demonstrated with api-authorization. Never infer full admin from one weak endpoint.
