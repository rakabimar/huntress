# GraphQL remediation

Enforce policy in shared services and each resolver path, scope DataLoader/cache keys by principal/tenant, define input types without protected fields, filter subscriptions at event time, apply bounded depth/field/cardinality cost controls, and test the resolver matrix across principals plus REST parity.
