# GraphQL false positives

Reject intentionally enabled introspection, public type/field names, global IDs that remain authorized, HTTP 200 with protected fields null/omitted and correct errors, ordinary alias/fragment/batch support, schema fields disabled by policy at execution, and theoretical depth/cost without an allowed measured boundary effect.
