# GraphQL evidence contract

Store minimal operation text, operation/field/resolver path, redacted variable shape, principal/tenant, exact node/object, owner and unauthorized results including partial data/errors, mutation postcondition or subscription event, schema/version context, and REST parity if relevant. Never persist tokens or broad enumerated data.
