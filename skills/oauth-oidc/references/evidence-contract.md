# OAuth/OIDC evidence contract

Include flow, actors, client/issuer/redirect identifiers, transaction and artifact fingerprints, expected binding, one substitution, attacker-controlled account/browser/endpoint, callback/token-validation outcome, resulting local account, and usable protected capability. Redact codes, tokens, cookies, email/subject values, and third-party data.
