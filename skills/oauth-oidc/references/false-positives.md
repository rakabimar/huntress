# OAuth/OIDC false positives

Reject cosmetic spec deviations, safely normalized/rejected redirects, state/nonce omissions where another evidenced binding prevents attacker control, PKCE behavior that cannot yield code theft, public client identifiers, expected IdP login selection, and tokens rejected by the resource server. An open redirect is only supporting evidence until code/token delivery is shown.
