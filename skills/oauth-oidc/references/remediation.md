# OAuth/OIDC remediation

Use exact registered redirects; transaction-bound state, nonce, and PKCE; issuer-aware authorization responses; single-use codes bound to client+redirect; strict token issuer/audience/type; stable issuer+subject account keys; recent re-authentication and uniqueness for linking; and end-to-end tests across every client/IdP/mobile variant.
