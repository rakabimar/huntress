name: false-positive-cicd-security
input: "A pull_request workflow checks out the contributor commit, but the job has read-only permissions, receives no secrets, and never feeds data to a privileged downstream workflow."
expected:
  promote_finding: false
  evidence_required: true
  tool_selection: none
  max_requests: 0
