# JWT remediation

Configure verification from trusted service policy: fixed algorithms, trusted issuer/key endpoints, bounded key identifiers, exact audience and token type/use, required temporal/subject claims, current server-side account/tenant state, short lifetimes, replay/refresh controls, and consistent libraries/configuration across gateway and services.
