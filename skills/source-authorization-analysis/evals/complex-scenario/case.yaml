name: complex-source-authorization-analysis
input: "A nested bulk endpoint authenticates at middleware, loads organization-scoped parents, then updates child IDs through an unscoped repository. Compare sibling paths and identify what would prove or disprove cross-tenant mutation."
expected:
  route: source-authorization-analysis
  hypothesis: "Create a source observation or Lead only after reachability and the security invariant are credible."
  refuting_observation: "A guard, safe wrapper, unreachable path, fixture status, or version mismatch disproves applicability."
  evidence_required: true
  promote_finding: false
  tool_selection: none
  source_runtime_correlation: required_when_hosted
  max_requests: 0
