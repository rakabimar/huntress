# Implementation notes

Review Socket.IO namespaces/rooms and middleware, ws upgrade handlers, Django Channels consumers/groups, Spring STOMP interceptors/destinations, Phoenix channels, GraphQL subscription context, and reverse-proxy upgrade forwarding. Never trust client-supplied user/room IDs or authorization performed only at connect.
