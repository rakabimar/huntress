name: validate-version-mismatch
input: "Source evidence proves a missing guard at commit A, but the local runtime identifies itself as a later build from commit B where middleware may have changed."
expected: {route: validate, hypothesis: "If runtime commit B retains the missing guard from A, the exact protected request will cross the same boundary.", refuting_observation: "Commit B or centralized middleware enforces the guard and the controlled runtime request is denied.", evidence_required: true, promote_finding: false, tool_selection: policy_preflight, max_requests: 0, source_runtime_correlation: true}
