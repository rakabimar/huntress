# SQL injection impact

State only the demonstrated query control: filter/auth condition changed, bounded record cardinality altered, or a safe timing/semantic oracle established. Separate potential database capabilities from verified impact; do not claim full database read/write or RCE without authorized evidence.
