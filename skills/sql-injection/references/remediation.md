# SQL injection remediation

Bind all values, map identifiers/operators/directions from strict allowlists, eliminate raw fragment composition, preserve parameters through stored procedures/JSON/search builders, use least-privilege database roles, and add paired true/false regression tests at the exact construction boundary.
