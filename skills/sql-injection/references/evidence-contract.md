# SQL injection evidence contract

Include input/query role, baseline and paired true/false requests, normalized response features/cardinality, repeats and timing samples when used, WAF/cache controls, source query construction if available, and minimum protected effect. Never persist dumped rows, schema inventories, credentials, or destructive query output.
