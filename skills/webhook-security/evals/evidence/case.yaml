name: evidence-webhook-security
input: "A signed payment webhook is valid for provider account A but names an order belonging to tenant B. The handler returns 200; no ledger state has been checked."
expected:
  evidence_required: true
  promote_finding: false
  tool_selection: none
  max_requests: 0
