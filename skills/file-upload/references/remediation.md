# File-upload remediation

Validate actual bytes with centralized allowlists, generate opaque storage keys, isolate storage and active rendering from application origin, set explicit content headers, sandbox/limit processors, contain archives by canonical path, bind direct-upload finalization and every variant to owner/tenant, and test the full lifecycle rather than upload only.
