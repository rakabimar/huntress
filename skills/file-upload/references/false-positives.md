# File-upload false positives

Reject files safely renamed and served as attachments, mismatched MIME rejected during finalize/processing, sanitized SVG/documents, isolated active content, parser errors without effect, archives contained under a generated root, direct uploads unusable before validated finalization, and public sharing or cross-user access explicitly intended.
