name: file-upload-tool-choice
input: "A signed direct-upload policy may allow a foreign tenant prefix; one controlled object write requires policy classification."
expected:
  tool_selection: policy_preflight
  evidence_required: true
  max_requests: 0
