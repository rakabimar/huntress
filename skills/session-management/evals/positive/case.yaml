name: routes-session-management-positive
input: "An in-scope example.test flow suggests session creation, rotation, expiry, and revocation; compare account_a baseline with one controlled mutation."
expected:
  route: session-management
  policy_preflight: true
  evidence_required: true

