# Session false positives

Reject intentionally concurrent devices, logout documented as current-device only, old tokens that decode but fail protected access, refresh retries handled idempotently, clock skew within policy, and cookie-attribute warnings without a feasible origin/subdomain threat. UI disappearance is not revocation evidence; conversely, a stale UI is not continued server authorization.
