# Session evidence contract

Store credential fingerprints/IDs only; class, device/account/AuthContext, issue/rotation/event/replay timestamps, cookie scope attributes, predecessor/successor relationship, expected invalidation semantics, protected replay request, and resulting authenticated identity/capability. Include a fresh-session control.
