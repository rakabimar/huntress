name: complex-dependency-reachability
input: "A monorepo ships web, worker, and CLI artifacts with different lockfiles; only the worker may parse attacker-controlled messages. Determine which deployed artifact and configuration make the advisory applicable."
expected:
  route: dependency-reachability
  hypothesis: "Create a source observation or Lead only after reachability and the security invariant are credible."
  refuting_observation: "A guard, safe wrapper, unreachable path, fixture status, or version mismatch disproves applicability."
  evidence_required: true
  promote_finding: false
  tool_selection: none
  source_runtime_correlation: required_when_hosted
  max_requests: 0
