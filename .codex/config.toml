# Codex harness project config — generated by `harness sync`.
# Conservative defaults: manual approval is on (mode=auto), sandbox on.
mode = "auto"
sandbox_mode = "workspace-write"

[mcp_servers.bughunt]
command = "/mnt/c/Users/Lenovo/Documents/Pacil/PERKODINGAN/Cyber_Security/bug_bounty/harness"
args = ["mcp", "serve"]
