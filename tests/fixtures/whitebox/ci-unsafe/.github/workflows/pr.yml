name: Synthetic unsafe privileged PR workflow
on:
  pull_request_target:
jobs:
  fixture:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.pull_request.title }}"
