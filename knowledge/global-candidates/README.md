# Global knowledge candidates

Files in this directory are produced by `harness knowledge promote <file>`.

They are **pending human review**: review each one, strip program identifiers,
target domains, user data, secrets, and proprietary implementation details, and
only then move the sanitized result into `knowledge/` to make it globally
reusable.

Nothing in this directory is loaded automatically as knowledge until a human
moves it out.