# JavaScript intelligence

Significant JavaScript is fetched through the Broker, hashed, and analyzed once per analysis version. Compact observations include candidate routes/URLs, WebSockets, GraphQL operations, source-map references, and secret fingerprints. Strings remain candidates. In-scope endpoints enter recon inventory with JavaScript artifact provenance, and changed bundle hashes can create recon changes. Bundles are not dumped into model context.
